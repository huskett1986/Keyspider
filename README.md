# Keyspider
Keyspider is a security-focused spidering tool for SMB shares and web applications. It searches filenames and file contents for user-defined keywords, generates directory maps for offline review, and crawls web applications while saving matching HTML pages locally.
